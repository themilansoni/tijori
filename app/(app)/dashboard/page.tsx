export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold">Dashboard</h1>
      <p className="mt-2 text-muted">
        Auth is wired up — summary cards, category spending, and charts land
        in a later phase.
      </p>
    </div>
  );
}
